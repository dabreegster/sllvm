########
# TODO #
########

# 1) Get all input to parse
  - scale... how to avoid reading whole file?

# 2) Get all echoed output to match input
  - var-args
  - unescape constant strings
  - module stores type table (and handles recursive types)

# 3) Make it clean
  - regroup to even get rid of named_inst and laters
  - fill out User operands
  - all instructions propagate parent BB to instruction children
  - anywhere with factories, override equals()

# 4) Make it fast
  - left factor the grammar... prevent backtracking
  - print progress without sacrificing perf
  - compare to native llvm

# 5) Make it pretty
  - better graphviz output
    - interprocedural caller/callee edges
  - wacky procedural generation visualization experiments

# 6) Make it legit
  - Build script(s)
  - Bigger tests
